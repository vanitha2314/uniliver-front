import { CustomDecimalPipePipe } from './custom-decimal-pipe.pipe';

describe('CustomDecimalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CustomDecimalPipePipe();
    expect(pipe).toBeTruthy();
  });
});
